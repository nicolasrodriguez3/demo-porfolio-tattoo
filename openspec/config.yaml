# openspec/config.yaml — SDD project configuration
# Auto-generated by sdd-init on 2026-07-07
schema: spec-driven

context: |
  Project: demo-portfolio-tattoo
  Client: Carmin Tattoo Studio, Bavio 173, Paraná, Entre Ríos, Argentina
  Stack: Astro + CMS headless (git-based) — planned, not yet scaffolded
  Type: Static multi-page website with CMS integration
  Features: Home, gallery with filters, artist profiles, contact with map, dark mode, lightbox
  Design: Dark aesthetic, strong typography, photo-driven layouts
  Status: Empty project — no package.json, no runtime config detected

testing:
  strict_tdd: false
  runner: none
  runner_command: ""
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
    command: ""
  linter:
    available: false
    command: ""
  type_checker:
    available: false
    command: ""
  formatter:
    available: false
    command: ""

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
